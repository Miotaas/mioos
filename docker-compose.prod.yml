version: "3.9"

# Production compose — binds only to localhost so Caddy can reverse-proxy.
# Usage:
#   docker compose -f docker-compose.prod.yml up -d --build

services:
  mioos:
    image: mioos:latest
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    # Only expose to localhost — Caddy terminates TLS and proxies here
    ports:
      - "127.0.0.1:3000:3000"
    volumes:
      - mioos_data:/data
    environment:
      - DATABASE_URL=file:/data/mioos.db
      - NODE_ENV=production
    env_file:
      - .env
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:3000/api/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 45s
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "5"

  mioos-runtime:
    image: mioos:latest
    # Override CMD to run the background worker instead of the web server
    command: ["npm", "run", "runtime"]
    restart: always
    depends_on:
      mioos:
        condition: service_healthy
    volumes:
      - mioos_data:/data
    environment:
      - DATABASE_URL=file:/data/mioos.db
      - NODE_ENV=production
    env_file:
      - .env
    # No ports — the runtime worker is internal-only
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

volumes:
  mioos_data:
    driver: local
